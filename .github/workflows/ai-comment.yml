# AI Commands — caller for lenaxia/ai-workflows.
#
# Delegates the slash-command AI flow (/ai, /fix, /implement, /review, /test,
# /security, /analyze, /explain, /triage, /design, /merge, /help) to the central
# reusable workflow, which owns prompt assembly (via route-command.sh), the
# opencode run, and the once-per-thread command footer.
#
# The command-token + author_association filter MUST live here (the caller) — a
# called workflow's job-level `if:` against github.event.comment.* is not
# reliably evaluated for issue_comment / pull_request_review_comment events.
#
# Pinned to ai-workflows v0.2.15 — bump by editing the tag below (or via propagate.yml).
# Requires OPENAI_API_KEY / OPENAI_API_BASE secrets + OPENAI_MODEL variable (inherited).

name: AI Commands

on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]

permissions:
  id-token: write
  contents: write          # write — this workflow allows code changes
  issues: write
  pull-requests: write

jobs:
  respond:
    if: |
      (github.event.comment.body == '/ai' || startsWith(github.event.comment.body, '/ai ') || contains(github.event.comment.body, ' /ai ')) ||
      (github.event.comment.body == '/review' || startsWith(github.event.comment.body, '/review ') || contains(github.event.comment.body, ' /review ')) ||
      (github.event.comment.body == '/fix' || startsWith(github.event.comment.body, '/fix ') || contains(github.event.comment.body, ' /fix ')) ||
      (github.event.comment.body == '/implement' || startsWith(github.event.comment.body, '/implement ') || contains(github.event.comment.body, ' /implement ')) ||
      (github.event.comment.body == '/analyze' || startsWith(github.event.comment.body, '/analyze ') || contains(github.event.comment.body, ' /analyze ')) ||
      (github.event.comment.body == '/test' || startsWith(github.event.comment.body, '/test ') || contains(github.event.comment.body, ' /test ')) ||
      (github.event.comment.body == '/security' || startsWith(github.event.comment.body, '/security ') || contains(github.event.comment.body, ' /security ')) ||
      (github.event.comment.body == '/explain' || startsWith(github.event.comment.body, '/explain ') || contains(github.event.comment.body, ' /explain ')) ||
      (github.event.comment.body == '/triage' || startsWith(github.event.comment.body, '/triage ') || contains(github.event.comment.body, ' /triage ')) ||
      (github.event.comment.body == '/help' || startsWith(github.event.comment.body, '/help ') || contains(github.event.comment.body, ' /help ')) ||
      (github.event.comment.body == '/design' || startsWith(github.event.comment.body, '/design ') || contains(github.event.comment.body, ' /design ')) ||
      (github.event.comment.body == '/merge' || startsWith(github.event.comment.body, '/merge ') || contains(github.event.comment.body, ' /merge ')) &&
      (github.event.comment.author_association == 'OWNER' ||
       github.event.comment.author_association == 'MEMBER' ||
       github.event.comment.author_association == 'COLLABORATOR')

    uses: lenaxia/ai-workflows/.github/workflows/ai-comment.yml@v0.2.15
    secrets: inherit
    with:
      version: v0.2.15
      project_name: mini4wd-track-editor
