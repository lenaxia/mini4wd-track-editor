{
  "name": "mini4wd-track-editor",
  "private": true,
  "type": "module",
  "description": "Touch-first Mini4WD track editor — the editor app is a zero-dependency static site (no build step); the optional storage server (server.js) defaults to zero-dep sqlite via node:sqlite and lazily imports pg (the sole runtime dependency) only for STORE=postgres.",
  "scripts": {
    "serve": "node serve.js",
    "server": "node server.js",
    "test": "node --test",
    "test:e2e": "playwright test",
    "manifest": "node tools/gen-manifest.js"
  },
  "dependencies": {
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
